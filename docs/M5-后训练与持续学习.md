# 🟡 M5-后训练与持续学习

> **核心问题：** 预训练模型怎么适配到特定硬件和任务？怎么保证"学会开这扇门"后不会"忘了怎么开那扇门"？
>
> **预计时间：** 2 周
>
> **状态：** 📝 待学习

---

## 1. 后训练的四种策略

### A. Parameter Merging（ICLR 2026）

```
方法：
  θ_merged = (1-α)·θ_finetuned + α·θ_pretrained
  
效果：
  → 同时保留通用能力 + 新任务能力
  → 在 OOD 变体上超过微调模型和预训练模型
  → 支持持续学习——加新任务不需要重新训练全部

优势：极其简单（一行代码），效果显著
```

### B. RL Fine-tuning

```
ReinFlow (NeurIPS 2025)：
  BC 预训练的 FM 策略 → 在线 RL 微调
  → 注入可学习噪声作探索
  → 操作任务 +40% 成功率
  → 比 Diffusion RL 省 23-83% wall time

FLaRe (ICRA 2025)：
  → 79.5% 长序列操作成功率
  → <1 天微调适配新具身
  → 只需稀疏奖励

关键：FM 比 Diffusion 更适合 RL 微调
      因为 ODE 采样更快 → RL 的交互-更新循环更快
```

### C. LoRA + 增量 MoE

```
LoRA：
  W' = W + A·B,  其中 A∈R^{d×r}, B∈R^{r×d}, r << d
  → 只训练 1% 参数
  → 多任务可切换（加载不同的 LoRA 权重）

增量 MoE：
  新任务 → 新 expert
  旧 expert 冻结
  Router 学习在任务间切换

  参考：MoT 的 Router 可以判断"这是开门的 token 还是拧瓶盖的 token"
```

### D. 交互修正（FlowCorrect, 2026）

```
不需要重新训练：
  部署时 → 人类通过 VR 给稀疏修正信号
  → 模型实时调整动作
  → 80% 修复已失败的 case

适用：那些"差一点就成功了但不想花时间重新训练"的场景
```

---

## 2. 力控风格微调

```
不同任务 → 不同力控风格：

  擦桌子：K 小（软、顺应），恒力 5N
  插销：  K 先大后小（先硬后软），力阈值触发
  抓鸡蛋：K 极小（极软），力上限 2N
  抓金属：K 大（硬），力上限 50N

做法：FM 不只输出位姿 → 还输出 (K(t), B(t), F_d(t))
     微调时只微调力控风格分支
```

---

## 3. 从开一扇门到开所有门——泛化

```
门的变异维度：尺寸、把手形状、推/拉、铰链位置

泛化策略：
  → Object-centric 表征：门 = 铰链 + 面板 + 把手（几何关系）
  → 力控泛化：门的力曲线形状本质相似
    （克服静摩擦 → 大力 → 动摩擦 → 小力）
  → 视觉泛化比力控泛化难得多
    → 你的策略应该"更信力触觉，更不信视觉"

关键挑战：从 50 条真机数据到泛化 → 数据增强 + 力控先验
```

---

## 4. 持续学习

```
灾难性遗忘：任务 B 微调 → 任务 A 成功率归零

解决方案优先级：
  1. Parameter Merging（最简单，ICLR 2026 验证有效）
  2. 增量 MoE（新任务=新 expert，冻结旧 expert）
  3. Replay（保留 5% 旧任务数据混合训练）
  4. EWC（惩罚重要权重的变化）
```

---

## 📚 学习资源

| 资源 | 说明 |
|------|------|
| 🎥 [Chelsea Finn: CS 330 — Transfer & Meta-Learning](https://www.youtube.com/results?search_query=cs330+transfer+learning+finn) | 迁移学习的数学基础 |

### 先读 3 篇

1. ⭐⭐⭐ "Robust Finetuning of VLA via Parameter Merging", ICLR 2026 — 最简单最有效的后训练
2. ⭐⭐⭐ ReinFlow, "Fine-tuning FM Policy with Online RL", NeurIPS 2025 — FM + RL 的证据
3. ⭐⭐⭐ FLaRe, "Large-Scale RL Fine-Tuning", ICRA 2025 — RL 微调的标杆

---

## ✍️ 写作练习

### L1 费曼笔记

- [ ] 用大白话解释：**Parameter Merging 为什么能防止灾难性遗忘？** 从权重空间的角度解释。

### L3 设计性写作

→ 写到 [I4-累积设计文档](/robotics-learning-map/I4-累积设计文档) Section 5（续）：你的后训练 + 持续学习方案

---

## 🔗 相关笔记

- [M4-预训练范式](/robotics-learning-map/M4-预训练范式) — 预训练之后的下一步
- [I4-累积设计文档](/robotics-learning-map/I4-累积设计文档)
- [写作工具箱](/robotics-learning-map/写作工具箱)
