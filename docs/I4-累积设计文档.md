# 🟢 I4-累积设计文档

> **这是你的「毕业论文」——不是一次写成的，是随着学习逐步迭代出来的。**
>
> **用法：** 每完成一个 Core/Methods 模块的 L3 练习，就更新对应的 section。不要追求一次完美——先写草稿，学了新东西再回来改。
>
> **状态：** 💡 等待你的第一版草稿
>
> **版本：** v0.0

---

## Section 1: 问题定义

> 学完 Core 层后填写

**应用场景：** [抓取？插销？灵巧手操作？...]

**输入模态：**

```
视觉：  [RGB-D? 几路相机? 手眼+第三视角?]
力觉：  [6-DoF? 采样率?]
触觉：  [什么传感器? 图像分辨率?]
本体：  [关节角+速度? 频率?]
任务：  [语言指令? 目标图像?]
```

**输出空间：**

```
M = SE(3) × R^6 × R^{fingers}
位姿：  [SE(3), 轨迹长度 H=?]
力目标：[R^6, 力/力矩约束?]
手指：  [关节角增量? latent action?]
```

**核心假设（为什么你的方法会 work）：**

1. [假设 1: 如——"SE(3) 上的 FM 比 R^6 上的 FM 生成的位姿更有效"]
2. [假设 2]
3. [假设 3]

---

## Section 2: FM 生成架构

> 学完 M1 后填写

**条件路径设计：**

```
SE(3) 上：  T_t = T₀ · exp(t · log(T₀⁻¹·T₁))
力空间：    F_t = (1-t)·F₀ + t·F₁
手指空间：  g_t = (1-t)·g₀ + t·g₁
```

**向量场网络 v_θ：**

```
输入： [位姿表征, 力表征, 手指表征, 观察嵌入, 时间 t]
输出： [se(3)切向量, 力增量, 手指增量]

Backbone： [MLP? DiT? Mamba?]
条件注入方式： [Cross-attention? FiLM? Concat?]
```

**Reflow 设计：**

```
Reflow 轮数：[1? 2? 3?]
配对生成策略：[从 p₀ 采样 → 跑当前 FM → 得到 x̂₁]
```

**蒸馏方案（可选）：**

```
教师： [50步 CFM]
学生： [单步预测]
蒸馏损失：[MSE? IMLE?]
```

---

## Section 3: 多模态融合

> 学完 M2 后填写

**Encoder 设计：**

```
视觉 encoder：[ResNet? ViT? 3D-aware? 预训练权重?]
力觉 encoder：[MLP, 几层? 是否加时序?]
触觉 encoder：[CNN? ViT? 预训练方法? 跨传感器通用?]
位姿 encoder：[se(3) local coords? 其他?]
```

**MoT 设计：**

```
总 expert 数：[]
Router 策略：[模态路由? 内容路由? 混合? Top-k=?]
专家分配：
  - Vision Expert: [] 个
  - Force Expert:  [] 个
  - Tactile Expert: [] 个
  - Shared Expert: [] 个
跨模态 Attention：[模态内? 跨模态? 只在某些层?]
频率对齐：[统一频率=?, 降采样/插值策略]
缺失模态：[MISSING token? 随机 drop 训练?]
```

---

## Section 4: 力控方案

> 学完 M3 后填写

**小脑设计：**

```
控制器类型：[阻抗? 导纳? 混合?]
阻抗参数：  K = [?] N/m, B = [?] Ns/m
参数是否学习：[固定? FM 输出? 单独网络?]
```

**触觉反射：**

```
反射类型：[硬编码规则? 轻量 MLP?]
触发条件：[滑移检测? 力超限? 接触丢失?]
响应：[力修正量? 紧急停止?]
延迟要求：[<5ms?]
```

**触觉预训练：**

```
方法：[MAE? 对比? 力预测?]
跨传感器：[GenForce 式迁移?]
数据集：[___]
```

---

## Section 5: 训练范式

> 学完 M4 + M5 后填写

**预训练：**

```
数据：
  - UMI: [] 条轨迹
  - ego: [] 条
  - dex: [] 条
  - 真机: [] 条
  - 仿真: [] 条

损失：
  L = L_BC + []·L_MAE + []·L_contrastive + []·L_world + []·L_force_pred

预训练轮数/步数：[]
硬件：[GPU数量/型号]
```

**后训练：**

```
硬件适配（全量微调）：
  - 数据量：[] 条真机轨迹
  - 冻结层：[___]

任务适配：
  - 方法：[ParamMerge? LoRA? RL?]
  - LoRA rank: []
  - RL 奖励：[稀疏? 密集? 设计?]

力控风格微调：
  - 微调分支：[仅力控 head? 还是也微调位姿?]
```

**持续学习：**

```
方法：[ParamMerge? 增量 MoE? Replay?]
旧任务数据保留比例：[5%? 10%?]
新增任务时：[冻结旧 expert? 全量更新?]
```

---

## Section 6: 系统架构

> 学完 I1 后填写

**大脑-小脑通信：**

```
通信方式：[ROS2 topic? Shared memory?]
话题/频率：
  - /fm_policy/targets @ [] Hz
  - /ft_sensor/wrench @ [] Hz
  - /tactile/image @ [] Hz
```

**FM 部署：**

```
推理步数：[1步蒸馏? 5步? 10步?]
推理硬件：[GPU型号, 推理延迟 ms]
流式：[Streaming Flow? 滚动时域?]
```

**安全层：**

```
力上限：[___ N]
紧急停止条件：[___]
不确定性阈值：[FM 的不确定性 > ___ → 降低 K]
```

---

## Section 7: 实验计划

> 学完 I3 后填写

**实验 1: SE(3) vs R^6**

```
场景：[]
Baseline：[R^6 FM]
指标：[成功率, 位姿误差, 旋转矩阵行列式(是否有效)]
预期：[SE(3) FM > R^6 FM, 尤其是在大旋转角任务]
```

**实验 2: 力触觉的价值**

```
场景：[]
Baseline：[无力触觉的 FM 策略]
指标：[成功率, 力跟踪误差, 滑移率, 过度用力率]
预期：[有力触觉 > 无力触觉, 差距在高精度任务中更大]
```

**实验 3: 泛化**

```
场景：[门→门? 物→物?]
指标：[OOD 成功率]
预期：[力控分支泛化 > 视觉分支泛化]
```

---

## 版本记录

```
v0.0 — 2026-06-06: 初始化，等待你的第一版草稿
```

---

## 🔗 相关笔记

- [00-总览-学习地图](/00-总览-学习地图)
- *C1-C4* — Core 层
- *M1-M5* — Methods 层
- *I1-I3* — Integration 层
