# 🟢 I3-评估与基准

> **核心问题：** 你怎么知道自己的模型好不好？用什么 benchmark、什么指标？消融实验怎么设计？
>
> **预计时间：** 1 周
>
> **状态：** 📝 待学习

---

## 1. 机器人操作 Benchmark

| Benchmark | 任务数 | 特点 |
|-----------|--------|------|
| **RLBench** | 100 | 单臂操作，覆盖抓取、推动、放置等 |
| **LIBERO** | 130 | 5 个子集，侧重泛化（空间、物体、目标） |
| **RoboCasa** | 100+ | 大规模日常操作，厨房/办公室场景 |
| **RoboChallenge** | 30 | "具身模型的全球考试"，真机评估 |
| **CALVIN** | 34 | 长序列任务，语言条件 |
| **ManiSkill** | 各种 | GPU 加速仿真，适合快速迭代 |

### 对你的意义

```
力触觉操作目前在 benchmark 中覆盖不足
  → 大部分 benchmark 只评估"位置到了没"
  → 力控质量、接触稳定性几乎没有标准 benchmark

→ 你的工作和评估体系可能需要自己定义
→ 或者改造现有 benchmark（如 LIBERO 加力传感器模拟）
```

---

## 2. 力触觉评估指标

### 位置指标（常用但不够）

```
成功率：完成了多少任务
位姿误差：最终位姿离目标位姿的测地线距离
轨迹平滑度：轨迹的 jerk（加加速度）

→ 这些只能告诉你"位置对不对"
→ 不能告诉你"力用得对不对"
```

### 力控指标（你需要补充的）

```
力跟踪误差：||F_actual - F_target|| / ||F_target||
接触稳定性：接触状态的切换次数（"去掉接触→重新接触"的频次）
过度用力率：F_actual > F_safe_threshold 的时间占比
滑移率：物体滑移事件数 / 操作时长
能量效率：总注入能量 / 任务完成所需最小能量估计
响应时间：接触力从 0 上升到目标值的时间
```

---

## 3. 消融实验设计

### 标准消融维度

```
模型组件消融：
  - FM on SE(3) vs FM on R^6（验证 SE(3) 的必要性）
  - 有力触觉 vs 无力触觉（验证力信号的贡献）
  - MoE vs 单一 FFN（验证 MoE 的贡献）
  - 有 Reflow vs 无 Reflow（验证路径拉直的效果）
  - 有力控风格微调 vs 无（验证力控微调的效果）

数据消融：
  - UMI only vs UMI+真机（验证多源数据的价值）
  - 数据量 100% → 50% → 10%（验证数据效率）
  - 真机数据量 10 → 25 → 50 → 100（验证 scaling）

蒸馏消融：
  - 50步 vs 10步 vs 5步 vs 1步（验证速度-质量 trade-off）
```

---

## 📚 先读

1. RoboChallenge leaderboard 页面 — 了解当前的 SOTA 和评估体系
2. DexForce (IEEE RAL 2025) — 看它用了什么力控指标

---

## 🔗 相关笔记

- [I4-累积设计文档](/I4-累积设计文档) — 你的实验中应该评估什么
